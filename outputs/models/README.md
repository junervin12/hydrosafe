# Model Artifacts

This folder is reserved for optional trained model artifacts generated by the reproducible workflow.

Large model files are excluded from the default repository package to keep the repository lightweight. If exact model replication is required, archive model artifacts externally using Zenodo, OSF, Figshare, institutional storage, or GitHub Releases, then document the DOI/URL and SHA256 checksums in `DATA_ACCESS.md`.